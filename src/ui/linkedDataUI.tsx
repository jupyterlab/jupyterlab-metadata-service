import * as React from 'react';

import { ReactWidget } from '@jupyterlab/apputils';

export class LinkedDataWidget extends ReactWidget {
  constructor() {
    super()
  }

  protected render() {
    return (
      <div></div>
    );
  }
}